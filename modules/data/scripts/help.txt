Data (YAML files in data/):
  data status               Show data files and entry counts
  data check                Run lint + referential integrity checks
  data lint                 Run schema validation only
  data edit                 Open data/ in $EDITOR
  data log                  Show git history for data/
  data diff                 Show uncommitted changes to data/
  data changelog <domain>   Show structured change history
